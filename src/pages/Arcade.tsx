import ArcadeScene from '../components/scene/ArcadeScene';

/**
 * Arcade Scene Page
 *
 * An immersive, first-person walk through the painted Sweetardio scene.
 * Players click the recreated glowing neon arrows (or use the keyboard) to
 * move from the shop, into the arcade, up to a machine, and on into the game.
 */
export default function ArcadePage() {
  return <ArcadeScene />;
}
